{% extends "base.jade" %}
{% block title %}重置密码{% endblock %}
{% block headright %}
a#font_color_white(href !="{%url registration_register%}")加入我们？注册
{% endblock %}
{% block content %}
div#wrap
    div#password_reset_content
        form(method="post",action="") {% csrf_token %}
            fieldset.success
                div(style="margin-left:35px;margin-top:35px")
                    font(size="4px")请输入您用于登录的邮箱，系统将发送密码重置信息至您的邮箱
                    div(style="padding-top:30px;padding-bottom:5px")
                        div.controls
                            {{ form.email }}
                            div#id_emailTip.onShow 请输入邮箱
                            {% if form.email.errors %}
                            p.errmsg#err_msg {{ form.email.errors|join:", " }}
                            {% else %}
                            p#err_msg &nbsp;
                            {% endif %}
                    div(style="margin-top:-10px")
                        button.btn.btn-success#button_color(type="submit")重置密码
                        div(style="margin:-30px 0px 0px 100px")
                            a(href !="{% url django.contrib.auth.views.login %}")
                                font(size="3px") >>返回登录界面
                    div(style="padding-top:30px")
                        font(color="#aaa",size="3px")如以上方法不能解决，请发送至service@6cans.com
{% endblock %}